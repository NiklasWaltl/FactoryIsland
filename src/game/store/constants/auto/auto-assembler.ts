// ============================================================
// Auto-assembler V1 — buffer and timing (no reducer import).
// ============================================================

/** Max iron ingots buffered per machine (covers 3-ingot recipe + small slack). */
export const AUTO_ASSEMBLER_BUFFER_CAPACITY = 5;
