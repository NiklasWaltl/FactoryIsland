// ============================================================
// Boost multiplier constants
// ------------------------------------------------------------
// Pure boost configuration values.
// MUST NOT runtime-import from ../reducer to avoid ESM cycles.
// ============================================================

/** Overclock multiplier for auto-miners. */
export const AUTO_MINER_BOOST_MULTIPLIER = 2;

/** Overclock multiplier for auto-smelters. */
export const AUTO_SMELTER_BOOST_MULTIPLIER = 2;